import DiscoveryFlow from '../components/discovery/DiscoveryFlow.jsx';

// Home is intentionally one focused path: ask, choose, reach out, sent.
// Session management remains available in Profile and Explorer.
export default function Dashboard() {
  return <DiscoveryFlow />;
}
